test.tha is example Theta code.
main.asm is Assembly code that the Theta code is trying to mimic.

As said before in the project's README, the script
does not create hardware specific code like headers,
so the compiled Theta code will still look very different,
and it will not be readily compileable.